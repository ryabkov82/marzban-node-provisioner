# Example .env for Makefile/script

INV=ansible/inventories/example.ini
# INV=ansible/inventories/ssh_config.yml

# Panel API
PANEL_URL=https://panel.example.com
PANEL_USERNAME=admin
PANEL_PASSWORD=S3cr3t
PANEL_VERIFY_TLS=true
# адрес для панели (опционально).
# если не задать, скрипт сам спросит у ssh: ssh -G mynode → HostName
# NODE_ADDRESS=203.0.113.10

# Target node defaults (for script targets)
SSH_TARGET=mynode       # алиас из ~/.ssh/config
SSH_KEY=~/.ssh/marzban_ci

# Optional script flags
# SKIP_UPGRADE=false
# SKIP_DOCKER=false
# SKIP_REGISTER=false
# DEBUG=1